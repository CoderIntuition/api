___stringToString(s) {
    if (s.length < 2 || !s.startsWith("\"") || !s.endsWith("\"")) {
        throw "Input " + str + " is not a valid string (missing quotes)";
    }
    return s.substring(1, s.length - 1);
}
